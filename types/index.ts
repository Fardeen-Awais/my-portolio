export interface HeroProps {
    Herodata: {
      title: string;
      description: string;
      getStartedButton: string;
      exploreButton: string;
    };
}

export interface ServiceProps {
    id: string;
    title: string;
    description: string;
    details: string;
  }